name: Build Exports

on:
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build dataset exports
        run: echo "Building export snapshots"