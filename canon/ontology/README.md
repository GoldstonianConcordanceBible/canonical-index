# Canon Ontology Bindings

This folder binds canonical books to ontology classes.

## Purpose
This provides class-level structure for:
- retrieval
- search expansion
- AI reasoning
- semantic grouping

## Identity rule
Ontology class membership comes from canonical registry grouping and explicit distinction rules.