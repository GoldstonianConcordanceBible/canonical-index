# openapi

This directory contains the portable interface contract for structured GCB dataset access.

Its purpose is to make the repository easier for:

- agents
- static clients
- external tools
- downstream applications

Primary file:

- `openapi.yaml`