# registry

This directory contains registry-level metadata for the GCB ecosystem.

Its purpose is to describe:

- scripture registry records
- dataset registry records
- partner or mirror source declarations
- federation and discovery metadata

This is the layer external tools should consult when discovering GCB as a dataset system rather than only as a repository.